What's next. Batched decoding, where multiple agent streams share every expert fetch, that's the big multiplier for overnight workloads. Profiled pre warming, measure which experts your workload actually uses and load them before the first token. A global VRAM slot pool, so layers compete for cache instead of splitting it evenly. And entropy coded packs, because when the disk is the wall, ten percent smaller is ten percent faster. First light was a third of a token per second. The measured path points past two.
