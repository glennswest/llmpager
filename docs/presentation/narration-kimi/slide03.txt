Why shouldn't this work? Because conventional serving loads every weight into GPU memory. At four bits, one trillion parameters needs five hundred seventy gigabytes, which is seven H two hundreds, a quarter million dollars of hardware. Even offloading to system RAM fails here, this box has one hundred twenty eight gigabytes, still four and a half times too small. But mixture of experts models have a secret. Each token only activates about three percent of the weights. Kimi picks eight experts out of three hundred eighty four, per layer. So the bet is simple. Treat the GPU like a cache, and the S S D like memory. Virtual memory paging, applied to a trillion parameters.
