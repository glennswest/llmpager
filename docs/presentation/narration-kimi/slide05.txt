Kimi did add real problems. Compared to Qwen, it's thirty three times more parameters, three times the experts, thirty seven times the pack size. It uses a completely different attention design called M L A, latent attention. A sigmoid router with bias correction instead of softmax. A shared expert that always runs. A dense first layer. And yarn scaling for a two hundred fifty six K context. The one gift in that list is M L A. Its whole memory state is two point three kilobytes per token. The experts are the entire challenge.
