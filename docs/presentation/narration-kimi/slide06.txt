M L A deserves a minute. DeepSeek style latent attention compresses each token's keys and values into a single five hundred seventy six float vector, shared by all sixty four heads. The per head keys and values are never materialized during decoding. Instead, the math is folded, absorbed, into the query once per token. On the memory side, I re quantize thirteen gigabytes of attention weights down to four bits at load time, and I exile the two gigabyte embedding table to system RAM, gathering one row per token. After all that, there's room for exactly four expert cache slots per layer. And that has to be enough.
