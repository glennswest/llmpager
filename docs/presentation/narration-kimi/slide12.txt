The RAM tier. Between the tiny VRAM cache and the enormous disk, there's now a managed eighty gigabyte cache in system memory. Three thousand two hundred experts, with frequency aware admission and eviction, at expert granularity, not the operating system's blind four kilobyte pages. Every disk read write allocates into the tier, so hot experts stop touching the disk entirely. A RAM hit costs two milliseconds instead of five. Thirteen percent of the experts fit, and because routing is heavily skewed, the effective hit rate runs far higher than thirteen percent.
