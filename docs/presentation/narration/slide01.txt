Hi — I'm Glen West. And this is LLM pager: a project about running mixture-of-experts language models that are bigger than your graphics card's memory. Specifically: a thirty-billion-parameter model — eighteen and a half gigabytes of weights — running, and even serving two models at once, on a sixteen-gigabyte consumer GPU, with the model's experts paged in from disk on demand.
