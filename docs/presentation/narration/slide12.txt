Then the performance work: from twenty tokens per second to forty one, a bit more than doubled. Each bar is one specific engineering change: vectorized memory loads in the math kernels, releasing cache slots with GPU events instead of stalls, batching all eight experts into single launches, serving the pack from a RAM tier, and a bit twiddling trick that halves the cost of unpacking four bit weights. Nothing here is a bigger GPU or a smaller model. And note the gray bar: the payjer can still feed one hundred thirteen, the headroom is real.
