The problem. mixture of experts models only activate a tiny fraction of themselves for each token, this model touches about three billion of its thirty billion parameters at a time. But conventional inference engines still demand that all the weights sit in GPU memory. Our card has sixteen gigabytes; the model needs eighteen and a half before we even talk about the working state. In plain terms: normally, that's game over, you buy a bigger GPU. But per word generated, the model only uses about a tenth of itself. That's the opening.
